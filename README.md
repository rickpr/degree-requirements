# Simple Application for Degree Requirements

To run this, do the normal Rails stuff:

``` bash
$ bundle
$ rake db:schema:load
```

I have provided an example degree program that may be seeded in:
``` bash
$ rake db:seed
```
