<%= bootstrap_form_for([@requirement, @edge]) do |f| %>
  <%= f.select :child_id, options_for_select(Requirement.all.map { |req| [req.name, req.id] }), label: 'Subrequirement' %>
  <%= f.primary "Add subrequirement" %>
<% end %>

<script>
$('select').select2();
</script>
